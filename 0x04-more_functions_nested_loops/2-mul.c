#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two numbers
 * @a: first int
 * @b: second int
 * Return: result
 */
int mul(int a, int b)
{
	int multiply = a * b;

	return (multiply);
}
