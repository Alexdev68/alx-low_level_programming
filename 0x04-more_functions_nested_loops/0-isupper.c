#include "main.h"

/**
 * _isupper - Returns 1 if uppercase then 0 if lowercase
 * @c: Integer
 * Return: 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'B')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
